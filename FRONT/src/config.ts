// URL base do backend. Em produção, troque pelo IP real do servidor.
export const API_URL = "http://localhost:3000/chat";

// Chave usada para salvar e carregar o histórico de conversas no AsyncStorage.
export const STORAGE_KEY = "@conversations";

// Chave usada para verificar se o usuário já viu o onboarding.
export const ONBOARDING_KEY = "@onboarding_done";
